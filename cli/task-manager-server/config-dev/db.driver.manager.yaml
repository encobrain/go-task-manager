drivers:
  postgres:
    db:
      type: "postgres"
      name: "task_manager_tasks"
    connect:
      host: "localhost"
      port: 5432
    auth:
      user: "postgres"
      pass: "aqw1346790"
  storage:
    db:
      type: "sqlite"
      name: "storage.db.sqlite3"
    extra:
      dirpath: "./"